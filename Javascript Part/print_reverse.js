    var coisas = ['1', '2'];
    var palavras = ['john', 'mary', 'chris'];

    function printReverse(params) {
        var index = params.length;
        for (i = index; i--;) {
            console.log(params[i]);
        }
    };




    printReverse(coisas);
    printReverse(palavras);